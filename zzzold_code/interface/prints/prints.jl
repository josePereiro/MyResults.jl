function print_numered_res_versions(working_dir, res_name)
    ver_paths = get_res_version_dir(working_dir, res_name);
    for (i, ver) in enumerate(ver_paths)
        desc_path =

        if i == 1

        else

        end
    end
end
