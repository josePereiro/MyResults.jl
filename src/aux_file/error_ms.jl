missing_aux_file_ms(file_path) = "$(file_path) not found!!! If this is a results repo it should "*
    "be solved by calling resolve, otherwise you must create a new repo with init_repo!!!";
const corrupted_aux_file_ms(file_path) = "$(file_path) is corrupted!!! "*
    "This should be solved calling resolve!!!";
